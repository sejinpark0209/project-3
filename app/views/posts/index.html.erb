<h1>Apartments</h1>


<td><%= link_to 'New', new_apartment_post_path %></td>
<tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= link_to 'Show', apartment_post_path(apartment.id, post.id) %></td>
        <!-- <td><%= link_to 'Edit', apartment_path(apartment.id) %></td> -->
        <!-- <td><%= link_to 'Destroy', apartment_path(apartment.id), method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
